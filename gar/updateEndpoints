#!/usr/bin/env node
// Usage:

var GlobalAssetRegistry = require("./globalassetregistry");


var polkadotEndpoint = {
    chainkey: 'polkadot-0',
    relaychain: 'polkadot',
    endpoints: ['wss://rpc.polkadot.io']
}
var kusamaEndpoint = {
    chainkey: 'kusama-0',
    relaychain: 'kusama',
    endpoints: ['wss://kusama-rpc.polkadot.io']
}

async function main() {
    var gar = new GlobalAssetRegistry();
    let relaychains = ['polkadot', 'kusama']
    let validParachainList = await gar.fetchParaIDs([polkadotEndpoint, kusamaEndpoint])
    await gar.updatePublicEndpoints(relaychains, validParachainList)
}

main()
    .then(() => {
        console.log("DONE")
        process.exit(0);
    })
    .catch((e) => {
        console.error('ERROR', e);
        process.exit(1);
    });
