#!/usr/bin/env node
 // Usage:  updateAddressBalances
var Manager = require("./manager");
var paraTool = require("./paraTool");
async function main() {
    var manager = new Manager();
    let chainID = -1;
    process.argv.forEach(function(val, index, array) {
        if (index == 2 && val.length > 0) {
            chainID = parseInt(val, 10);
        }
    });
    if ( chainID >= 0 ) {
	await manager.update_address_balances_logDT(chainID);
    } else {
	while ( 1 ) {
	    let res = await manager.updateAddressBalances();
	    if ( res == false ) {
		await manager.sleep(30000);
	    } else {
		await manager.sleep(5000);
	    }
	}
    }
}

main()
    .then(() => {
        process.exit(0);
    })
    .catch((e) => {
        console.error('ERROR', e);
        process.exit(1);
    });
